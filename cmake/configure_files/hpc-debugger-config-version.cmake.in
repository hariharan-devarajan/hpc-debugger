#-----------------------------------------------------------------------------
# Version file for install directory
#-----------------------------------------------------------------------------
set(PACKAGE_VERSION @HPC_DEBUGGER_PACKAGE_VERSION@)

if ("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL @HPC_DEBUGGER_VERSION_MAJOR@)
    set(PACKAGE_VERSION_COMPATIBLE 1)
    if ("${PACKAGE_FIND_VERSION_PATCH}" EQUAL @HPC_DEBUGGER_VERSION_PATCH@)
        set(PACKAGE_VERSION_EXACT 1)
    endif ()
endif ()